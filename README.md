# ATProto community projection demo

This repository publishes a deterministic, synthetic-only SCV0 projection
artifact. It demonstrates one narrow product claim: a community can own its
inclusion decisions while a replaceable renderer remains only a window onto
those decisions.

Nothing here came from a live account or Space. The site contains no OAuth
token, Space credential, private source data, moderator capability, analytics,
cookies, or network client.

The exact artifact was generated by
[`unpingable/atproto-community`](https://github.com/unpingable/atproto-community)
at source commit `6416d54`, using the `community-space-demo` compiler.

Bundle ID:
`sha256:9bc2a8935f1f239fbcd311287fcf3602fa4a7f1ebfd9bcada0d413dc26455b5e`

Verify all generated payloads with:

```sh
sha256sum -c checksums.txt
```

The generated manifest records the exact claims and non-claims. In
particular, publication does not prove reader authorization, membership,
moderator or governance authority, source truth, repository currentness,
human usability, or production readiness.

## License

Dual-licensed under Apache-2.0 or MIT, at your option. See
`LICENSE-APACHE` and `LICENSE-MIT`.
